import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import { default as meta, InitialState } from './FollowButton.stories'

<Meta of={meta} />

# Follow Button & Transaction Modal

Follow/unfollow button wired to EFP onchain transactions. Requires **Wagmi**, **TransactionProvider**, and renders the **Transaction Modal** for batch flows.

[Follow Button docs →](https://ethidentitykit.com/docs/follow-button) · [Transaction Modal →](https://ethidentitykit.com/docs/transaction-modal)

<Canvas of={InitialState} />
