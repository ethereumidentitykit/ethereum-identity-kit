import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import { default as meta, Default } from './TranslationDemo.stories'

<Meta of={meta} />

# Translation Provider

`TranslationProvider` supplies i18n strings to EIK components. Use the toolbar **Language** control in this playground to switch locales live.

[Full docs →](https://ethidentitykit.com/docs/translation-provider)

<Canvas of={Default} />
