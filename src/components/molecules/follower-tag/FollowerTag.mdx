import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import { default as meta, ByAddress } from './FollowerTag.stories'

<Meta of={meta} />

# Follower Tag

Relationship badge between the connected wallet and a profile (follows you, mutual, blocked, etc.).

[Full docs →](https://ethidentitykit.com/docs/follower-tag)

<Canvas of={ByAddress} />
