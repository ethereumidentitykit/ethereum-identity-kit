import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import { default as meta, PlainProfile } from './ProfileListRow.stories'

<Meta of={meta} />

# Profile List Row

Single row for use inside profile lists — avatar, name, and optional follower tags.

[Full docs →](https://ethidentitykit.com/docs/profile-list-row)

<Canvas of={PlainProfile} />
